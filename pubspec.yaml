name: budget_mate_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  uuid: ^4.4.0
  provider: ^6.1.2
  intl: ^0.20.2
  fl_chart: ^0.68.0
  shared_preferences: ^2.2.0
  http: ^0.13.6 # ✅ Tambahkan ini untuk request ke backend API

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
